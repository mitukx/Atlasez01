---
/** 学習サイト「アトラス」の共通レイアウト（ロケール対応） */
import BaseHead from "../components/BaseHead.astro";
import UiLanguage from "../components/UiLanguage.astro";
import SettingsMenu from "../components/SettingsMenu.astro";
import LangSwitch from "../components/LangSwitch.astro";
import HeaderSearch from "../components/HeaderSearch.astro";
import { withBase, atlasPath, ORG } from "../lib/site";
import { t, type Locale } from "../lib/i18n";

interface AlternateLink {
  hreflang: string;
  href: string;
}
interface Props {
  locale: Locale;
  title: string;
  description: string;
  ogType?: "website" | "article";
  alternates?: AlternateLink[];
  structuredData?: object;
  /** 言語切替リンク先（省略時は各ロケールの総合ホーム） */
  localeSwitchHrefs?: Record<string, string>;
  /** 検索エンジンにインデックスさせない（内容が空のページなど） */
  noindex?: boolean;
}
const {
  locale,
  title,
  description,
  ogType,
  alternates = [],
  structuredData,
  localeSwitchHrefs = {},
  noindex = false,
} = Astro.props;

/**
 * ナビゲーション。
 * 行き先は3つに絞ってヘッダーを1段にした。
 * - 学習地図はトップページの表示タブから開けるのでここには置かない
 * - あとで読む・学習の記録は「学習リスト」1ページにまとめた
 * 検索は「行き先」ではなく「操作」なので、タブではなく入力欄として同じ段に置く。
 * 「はじめての方へ」は入口として目立たせたいので囲みスタイルにする。
 */
const nav = [
  { label: t(locale, "subjects"), href: atlasPath(locale) },
  { label: t(locale, "learningList"), href: atlasPath(locale, "list") },
];
const guide = {
  label: t(locale, "forBeginners"),
  href: atlasPath(locale, "guide"),
};
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang={locale} data-site="atlas">
  <head>
    <BaseHead
      title={`${title} | ${t(locale, "siteName")}`}
      description={description}
      ogType={ogType}
      alternates={alternates}
      structuredData={structuredData}
      noindex={noindex}
    />
  </head>
  <body>
    <UiLanguage />
    <a class="skip-link" href="#main">{t(locale, "skipToContent")}</a>
    <header class="atlas-header">
      {/* ロゴ・行き先・はじめての方へ・検索・表示設定を1段に並べる */}
      <div class="container atlas-bar">
        <a class="atlas-logo" href={atlasPath(locale)}>
          <img
            src={`${import.meta.env.BASE_URL.replace(/\/$/, "")}/images/atlasez-logo.png`}
            alt=""
            width="40"
            height="40"
          />
          <span>{locale === "ja" ? "アトラス" : "Atlas"}</span>
        </a>
        <nav
          class="main-nav"
          id="atlas-main-nav"
          aria-label={locale === "ja"
            ? "メインナビゲーション"
            : "Main navigation"}
        >
          <ul class="atlas-nav">
            {
              nav.map((item) => (
                <li>
                  <a
                    href={item.href}
                    aria-current={currentPath === item.href ? "page" : undefined}
                  >
                    {item.label}
                  </a>
                </li>
              ))
            }
            <li class="nav-guide">
              <a
                href={guide.href}
                aria-current={currentPath === guide.href ? "page" : undefined}
              >
                {guide.label}
              </a>
            </li>
          </ul>
        </nav>
        <div class="atlas-bar-search">
          <HeaderSearch locale={locale} />
        </div>
        <div class="atlas-bar-actions">
          <SettingsMenu locale={locale} />
        </div>
      </div>
    </header>
    <main id="main">
      <slot />
    </main>
    <footer class="atlas-footer">
      <div class="container atlas-footer-inner">
        <div>
          <p class="atlas-footer-title">{t(locale, "siteName")}</p>
          <p>{t(locale, "siteTagline")}</p>
          <p>
            {t(locale, "operatedBy")}:
            <a href={withBase("/")}>Atlasez</a> ・
            <a href={ORG.atlasSns.x} rel="noopener noreferrer" target="_blank"
              >X (@learning_atlas)</a
            >
          </p>
          <p>
            <a href={withBase("/privacy-policy/")}>
              {locale === "ja" ? "プライバシーポリシー" : "Privacy Policy"}
            </a>
          </p>
        </div>
        <div class="atlas-footer-language">
          <LangSwitch locale={locale} hrefs={localeSwitchHrefs} />
        </div>
      </div>
      <p class="container atlas-copyright">© 2024-2026 Atlasez</p>
    </footer>
  </body>
</html>

<style>
  .atlas-header {
    border-top: 3px solid var(--accent-primary);
    border-bottom: 1px solid var(--border-default);
    background: var(--background-primary);
  }
  /* 1段ヘッダー。添付デザインに合わせ、ロゴ・行き先・検索・表示設定の順 */
  .atlas-bar {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding-block: var(--space-1);
  }
  .atlas-logo {
    order: 0;
  }
  .main-nav {
    order: 1;
    min-width: 0;
  }
  .atlas-bar-search {
    order: 2;
    flex: 1 1 20rem;
    min-width: 0;
    display: flex;
    justify-content: flex-end;
  }
  .atlas-bar-actions {
    order: 3;
    display: flex;
    align-items: center;
    gap: var(--space-2);
    flex: 0 0 auto;
  }
  .atlas-logo {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-weight: 700;
    font-size: calc(1.15rem * var(--text-scale));
    color: var(--accent-primary);
    text-decoration: none;
    min-height: var(--touch-target);
  }
  .atlas-nav {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-1);
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .atlas-nav a {
    display: inline-flex;
    align-items: center;
    min-height: var(--touch-target);
    padding: 0 var(--space-2);
    text-decoration: none;
    color: var(--text-primary);
    border-bottom: 2px solid transparent;
  }
  .atlas-nav a:hover,
  .atlas-nav a[aria-current="page"] {
    border-bottom-color: var(--accent-primary);
    color: var(--accent-primary);
  }
  /* 「はじめての方へ」は添付デザインと同じ囲みスタイル */
  .nav-guide {
    display: flex;
    align-items: center;
  }
  .nav-guide a {
    border: 1px solid var(--accent-primary);
    border-radius: var(--radius);
    border-bottom-width: 1px;
    background: var(--accent-primary);
    color: var(--on-accent);
    font-weight: 700;
    padding: 0 var(--space-3);
    margin-block: var(--space-1);
  }
  .nav-guide a:hover,
  .nav-guide a[aria-current="page"] {
    background: color-mix(in srgb, var(--accent-primary) 86%, black);
    color: var(--on-accent);
    border-color: var(--accent-primary);
  }
  main {
    min-height: 60vh;
    padding-block: var(--space-3);
  }
  .atlas-footer {
    border-top: 1px solid var(--border-default);
    background: var(--background-primary);
    padding-block: var(--space-5);
    font-size: calc(0.9rem * var(--text-scale));
  }
  .atlas-footer-inner {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(16rem, 1fr));
    gap: var(--space-4);
  }
  .atlas-footer-title {
    font-weight: 700;
    margin-top: 0;
  }
  .atlas-footer-language {
    display: flex;
    justify-content: flex-end;
  }
  .atlas-copyright {
    margin-top: var(--space-4);
    color: var(--text-secondary);
  }
  /* 狭い画面では1段に収まらないので3段に折る。畳まず全部出す。
     1段目: アトラス・表示設定 / 2段目: 検索欄 / 3段目: 行き先
     行き先は3つなので、常に見えていても1行に収まる。 */
  @media (max-width: 900px) {
    .atlas-bar {
      gap: var(--space-2);
      flex-wrap: wrap;
      row-gap: var(--space-1);
    }
    .atlas-logo {
      order: 0;
      margin-right: auto;
    }
    .atlas-bar-actions {
      order: 1;
      margin-left: 0;
    }
    /* スマホの1段目は「アトラス」と「表示設定」だけにする */
    .atlas-bar-search {
      order: 2;
      flex: 0 0 100%;
    }
    .main-nav {
      order: 3;
      width: 100%;
      padding-top: var(--space-1);
      border-top: 1px solid var(--border-default);
    }
    .atlas-nav {
      display: grid;
      /* 分野・学習リスト・はじめての方へ の3つを均等に並べる */
      grid-template-columns: repeat(3, minmax(0, 1fr));
      width: 100%;
      gap: var(--space-1);
    }
    .atlas-nav a {
      width: 100%;
      justify-content: center;
      padding-inline: var(--space-1);
      font-size: calc(0.9rem * var(--text-scale));
    }
    .nav-guide a {
      justify-content: center;
    }
  }
</style>
