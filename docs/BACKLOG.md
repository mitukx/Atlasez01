# 積み残し（着手前のメモ）

やると決めているが、まだ手をつけていないものを置く。
片付いたら行ごと消す。

## 1. `src/data/atlas-members.json` の扱い

93名分の氏名などを含むファイルが公開リポジトリに残っている。
サイトからは参照していない（運営紹介ページは削除済み）。

- やること: 削除の可否を確認する。削除する場合は履歴からも消すか判断が要る。

## 2. 学習リストの同期

現状、あとで読む・学習の記録はこの端末の localStorage にだけ保存される。
書き出し・読み込み（JSON）は実装済みなので端末間の持ち運びはできるが、自動同期はしない。

- やること: 端末をまたいだ同期が要るかを見極める。要るならログインの導入が前提になる。

## 3. 総論記事の書き足し

7分野の「〇〇とは」は入口の説明までしか書いていない。
各記事の末尾「これから書き足すこと」に、続きの見出し案を挙げてある。

- 置き場所: `src/content/articles/ja/<分野>/overview/what-is-<分野>.md`
- 飛び先の指定: `subjects.yaml` の `introSlug`

## 4. トップページのHTMLが重い

概念表とリスト表示を地図から外して 166KB → 128KB まで下がったが、
経路検索の選択肢（161項目×2）とオートコンプリートの候補がまだ埋め込まれている。

- やること: 選択肢を graph.json から組み立てるか、地図タブを別ページに切り出す。

## 5. 学習地図の代替表示

概念マップはJavaScriptが要る。以前あったリスト表示・表形式は横幅を食うので外した。
いまは JavaScript が無い場合、トップページの「リスト表示」に案内している。

- やること: これで足りるかを見極める。足りなければ地図の下に簡素な一覧を戻す。
